# Step 2 — Tokenizer

Goal: build a **character-level** tokenizer.

- `tokenizer.py` defines `CharTokenizer`, which maps characters to integer IDs (`stoi`) and back (`itos`).
- `test_tokenizer.py` is a small sanity script.

Run (from repo root):
- `python step2_tokenizer/test_tokenizer.py`
